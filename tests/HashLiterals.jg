# jgtest exec on


main() Int {
    hash = { 'a': 'one', 'b': 'two' }
    for pair in hash {
        Io::println("#{pair.first} => #{pair.second}")
    }

    hash = Hash[String,String]()
    for pair in hash {
        Io::println("#{pair.first} => #{pair.second}")
    }
    ret 0
}
