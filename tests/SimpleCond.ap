# aptest run_program on
import Boot

main() Int {
    a = true
    b = false
    
    # Test true/false branching with no nesting
    if a {
        boot_print_str("true\n")
    } else {
        boot_print_str("false\n")
    }

    if b {
        boot_print_str("true\n")
    } else {
        boot_print_str("false\n")
    }

    return 0
}
