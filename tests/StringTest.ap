# aptest print_ast on

Stringable < Object {
    str?() String {
        ret "is"
    }
}

main() Int {
    bob = "what #{Stringable()} up"
    bob = "what #{ "what #{Stringable()} up"} up"
    bob = "what #{Stringable()} up what #{Stringable()} up"
    ret 0
}
