# jgtest exec on

import Func

main() Int {
    arr = ["red", "orange", "yellow", "green", "blue", "indigo", "violet"]
    arr2 = take(arr, 3)
    for x in arr2 {
        Io::println(x)
    } 
    ret 0
}
