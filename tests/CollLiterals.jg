# jgtest exec on

ArrayLike < Object {

    push(elem String) {
        Io::println(elem.str)
    }
}

print(arr Array[String]) {
    for x in arr {
        Io::println(x)
    }
}

main() Int {
    list = ["1", "2", "3", "4"]
    for x in list {
        Io::println(x)
    } 

    list = []
    for x in list {
        Io::println(x)
    } 

    list = ["a", "b"]+["c", "d"]
    for x in list {
        Io::println(x)
    }
    print(["hello"]+["world"])

    ar ArrayLike = ["alpha", "beta", "gamma"]  
    ret 0
}
