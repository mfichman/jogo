# aptest run_program on

import Boot

fib(n Int) Int {
    if (n == 0) { 
        return 0
    }
    if (n == 1) {
        return 1
    }
    return fib(n-1) + fib(n-2)
}


main() Int {
    x = fib(9)
    boot_print_int(x)   
    boot_print_str("\n")
    return 0
}
