
fib(n Int) Int {
    if n == 0 { 
        ret 0
    }
    if n == 1 {
        ret 1
    }
    ret fib(n-1) + fib(n-2)
}


main() Int {
    n = Io::stdin().scanln().int
    Io::println(fib(n).str)
    ret 0
}
